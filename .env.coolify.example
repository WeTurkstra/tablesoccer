# Coolify Environment Variables
# ============================
# Copy these variables to Coolify's Environment Variables section in your project settings
# Replace example values with your actual production values

# REQUIRED VARIABLES
# ------------------

# Your domain name (without https://)
# Example: tablesoccer.yourdomain.com or app.example.com
SERVER_NAME=your-domain.com

# Application secret key - MUST be a secure random string (min 32 characters)
# Generate with: openssl rand -hex 32
APP_SECRET=REPLACE_WITH_SECURE_RANDOM_STRING_32_CHARS_MIN

# Mercure JWT secret for real-time updates - MUST be a secure random string
# Generate with: openssl rand -hex 32
CADDY_MERCURE_JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_STRING_32_CHARS_MIN

# Database configuration
POSTGRES_DB=tablesoccer
POSTGRES_USER=tablesoccer_user
POSTGRES_PASSWORD=REPLACE_WITH_SECURE_DATABASE_PASSWORD


# OPTIONAL VARIABLES (with defaults)
# -----------------------------------

# PostgreSQL version (default: 16)
POSTGRES_VERSION=16

# HTTP/HTTPS ports (defaults: 80, 443)
# Note: Coolify typically handles port mapping automatically
HTTP_PORT=80
HTTPS_PORT=443
HTTP3_PORT=443

# Docker image prefix (leave empty unless using a custom registry)
IMAGES_PREFIX=


# DEPLOYMENT NOTES
# ----------------
# 1. Generate secure random strings for APP_SECRET and CADDY_MERCURE_JWT_SECRET
#    You can use: openssl rand -hex 32
#
# 2. Set SERVER_NAME to your actual domain (Coolify will handle DNS/SSL)
#
# 3. Create strong database credentials
#
# 4. In Coolify, go to your project > Environment Variables and add these
#
# 5. Deploy using: compose.coolify.yaml
